#ifndef LIB_RSDEVICE_HPP
#define LIB_RSDEVICE_HPP

#include <chrono>

namespace RS {

enum class DeviceState {
	Working,
	Suspended,
	Disabled
};

template<typename T>
class RsDeviceDescriptor {
public:
private:
	std::chrono::milliseconds



};

}

#endif // RSDEVICE_HPP
